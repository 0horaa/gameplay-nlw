const DATABASE_NAME = "@gameplay";

//@nome_do_app - :coleção - isso é só um padrão de código pra ficar mais bonitinho

export const COLLECTION_USERS = `${DATABASE_NAME}:user`;
export const COLLECTION_APPOINTMENTS = `${DATABASE_NAME}:appointments`;